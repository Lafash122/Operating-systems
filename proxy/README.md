## A non-caching multithreaded HTTP proxy (HTTP 1.0 version).
The proxy accepts connections on port 80 and forwards them to the required server.
All connection processing takes place in a separate thread.
